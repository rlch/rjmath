import 'package:flutter/material.dart';

class TextNode extends StatelessWidget {
  const TextNode({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
